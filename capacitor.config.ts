import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ihackfx.watchlist',
  appName: 'WatchList',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
